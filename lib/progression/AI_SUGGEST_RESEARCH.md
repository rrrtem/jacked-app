# Research: AI Suggested Workout Logic

## 📚 Научная база для умных рекомендаций

### 1. Восстановление мышц

**Временные рамки восстановления по группам мышц:**

| Группа мышц | Время восстановления | Оптимальная частота |
|-------------|---------------------|---------------------|
| **Грудь** | 48-72 часа | 2-3 раза в неделю |
| **Спина** | 48-72 часа | 2-3 раза в неделю |
| **Ноги** (квадрицепс, бицепс бедра) | 72-96 часов | 2 раза в неделю |
| **Плечи** | 48 часа | 2-3 раза в неделю |
| **Руки** (бицепс, трицепс) | 48 часа | 2-3 раза в неделю |
| **Кор** (пресс, поясница) | 24-48 часов | 3-4 раза в неделю |

**Ключевые факторы:**
- Крупные мышечные группы требуют больше времени на восстановление
- Высокоинтенсивные тренировки (тяжелый вес, низкие повторы) требуют больше времени
- Возраст влияет на скорость восстановления (с возрастом нужно больше времени)

### 2. Минималистичная философия тренировок

**Принцип "Big 3" (Основная тройка):**
1. **Приседания** (Squat) - работают ноги, кор, спина
2. **Становая тяга** (Deadlift) - работают спина, ноги, кор, хват
3. **Жим лежа** (Bench Press) - работают грудь, плечи, трицепс

**Расширенная версия "Big 5":**
4. **Жим стоя** (Overhead Press) - плечи, трицепс, кор
5. **Тяга штанги в наклоне** (Barbell Row) - спина, бицепс, кор

**Почему это работает:**
- Комплексные (compound) упражнения задействуют несколько суставов и групп мышц
- Эффективное использование времени
- Естественные движения, которые развивают функциональную силу
- Меньше риска "тренировочного выгорания"

### 3. Паттерны движений

**Основные паттерны:**
1. **Push (Толкай):**
   - Горизонтальный: жим лежа, отжимания
   - Вертикальный: жим стоя, жим гантелей

2. **Pull (Тяни):**
   - Горизонтальный: тяга штанги, тяга гантелей
   - Вертикальный: подтягивания, тяга верхнего блока

3. **Squat (Приседай):**
   - Присед со штангой, фронтальный присед, гоблет-присед

4. **Hinge (Наклон с прямой спиной):**
   - Становая тяга, румынская тяга, гиперэкстензия

5. **Carry (Перенос):**
   - Прогулка фермера, кубковый присед

6. **Core (Стабилизация):**
   - Планка, боковая планка, палофф-пресс

### 4. Принципы чередования

**Антагонистические пары (мышцы-антагонисты):**
- Грудь ↔ Спина
- Бицепс ↔ Трицепс
- Квадрицепс ↔ Бицепс бедра
- Пресс ↔ Поясница

**Логика чередования:**
- После тренировки груди (push) → следующая тренировка спина (pull)
- После тяжелых ног → как минимум 72 часа отдыха перед следующей тренировкой ног
- Можно тренировать верх после тренировки низа в тот же день

**Популярные сплиты для минималистов:**

1. **Full Body (3 раза в неделю):**
   - День A: Присед, Жим лежа, Тяга в наклоне
   - День B: Становая тяга, Жим стоя, Подтягивания
   - День C: Повтор дня A с другими весами

2. **Upper/Lower (4 раза в неделю):**
   - Верх A: Жим лежа, Тяга в наклоне, Жим стоя
   - Низ A: Присед, Румынская тяга, Выпады
   - Верх B: Жим гантелей, Подтягивания, Жим узким хватом
   - Низ B: Становая, Присед фронтальный, Сгибания ног

3. **Push/Pull/Legs (3-6 раз в неделю):**
   - Push: Жим лежа, Жим стоя, Жим гантелей
   - Pull: Становая, Тяга в наклоне, Подтягивания
   - Legs: Присед, Румынская тяга, Выпады

### 5. Объем тренировок

**Рекомендуемый объем (подходы × повторы в неделю):**

| Группа мышц | Минимум | Оптимум | Максимум |
|-------------|---------|---------|----------|
| Грудь | 10 подходов | 15-20 подходов | 25 подходов |
| Спина | 12 подходов | 18-22 подхода | 28 подходов |
| Ноги | 12 подходов | 16-20 подходов | 25 подходов |
| Плечи | 8 подходов | 12-16 подходов | 22 подхода |
| Руки | 6 подходов | 10-14 подходов | 20 подходов |

**Для минималистичного подхода:**
- 2-3 упражнения по 3-4 подхода = 6-12 подходов на группу
- Это находится в диапазоне минимум-оптимум
- Достаточно для прогресса при правильной интенсивности

### 6. Прогрессивная перегрузка

**Принципы прогресса:**
1. **Увеличение веса** - основной метод (2.5-5 кг когда можешь сделать верхнюю границу повторов)
2. **Увеличение повторов** - если не можешь добавить вес
3. **Увеличение подходов** - если застой (но не более 5 подходов)
4. **Улучшение техники** - всегда в приоритете
5. **Увеличение частоты** - тренировать группу чаще

## 🤖 Алгоритм AI Suggested

### Входные данные:
1. История тренировок пользователя за последние 14 дней
2. Дата последней тренировки каждой группы мышц
3. Какие упражнения делались недавно
4. Доступное оборудование
5. Предпочтения пользователя

### Логика работы (не AI, а правила):

#### Шаг 1: Определить, какие группы мышц готовы к тренировке

```typescript
function getMusclesReadyToTrain(workoutHistory: WorkoutHistory[]) {
  const today = new Date()
  const readyMuscles: MuscleGroup[] = []
  
  for (const muscleGroup of ALL_MUSCLE_GROUPS) {
    const lastWorkout = getLastWorkoutForMuscle(muscleGroup, workoutHistory)
    
    if (!lastWorkout) {
      readyMuscles.push(muscleGroup) // никогда не тренировали
      continue
    }
    
    const daysSinceLastWorkout = daysBetween(lastWorkout.date, today)
    const recoveryTime = RECOVERY_TIMES[muscleGroup]
    
    if (daysSinceLastWorkout >= recoveryTime) {
      readyMuscles.push(muscleGroup)
    }
  }
  
  return readyMuscles
}
```

#### Шаг 2: Выбрать тип тренировки

```typescript
function selectWorkoutType(readyMuscles: MuscleGroup[], lastWorkouts: Workout[]) {
  // Анализируем последние 3 тренировки
  const last3 = lastWorkouts.slice(0, 3)
  
  // Если последние 2 тренировки были верх - делаем низ
  if (last3.slice(0, 2).every(w => isUpperBody(w))) {
    return 'lower_body'
  }
  
  // Если последние 2 были низ - делаем верх
  if (last3.slice(0, 2).every(w => isLowerBody(w))) {
    return 'upper_body'
  }
  
  // Если последняя была push - делаем pull
  if (last3[0] && isPush(last3[0])) {
    return 'pull'
  }
  
  // Если последняя была pull - делаем push или legs
  if (last3[0] && isPull(last3[0])) {
    return readyMuscles.includes('legs') ? 'legs' : 'push'
  }
  
  // По умолчанию - full body
  return 'full_body'
}
```

#### Шаг 3: Подобрать упражнения

```typescript
function suggestExercises(
  workoutType: WorkoutType,
  recentExercises: Exercise[],
  availableEquipment: Equipment[]
): Exercise[] {
  const exercises: Exercise[] = []
  
  // Исключаем упражнения, которые делались в последние 2 тренировки
  const recentExerciseIds = recentExercises.slice(0, 2 * 3).map(e => e.id)
  
  // Правило 1: Всегда начинаем с комплексного упражнения
  const mainCompound = selectMainExercise(workoutType, recentExerciseIds)
  exercises.push(mainCompound)
  
  // Правило 2: Добавляем вспомогательное комплексное
  const secondaryCompound = selectSecondaryExercise(
    workoutType, 
    [...recentExerciseIds, mainCompound.id]
  )
  exercises.push(secondaryCompound)
  
  // Правило 3: Опционально добавляем одно изолирующее (если нужно)
  if (shouldAddIsolation(workoutType, exercises)) {
    const isolation = selectIsolationExercise(workoutType)
    exercises.push(isolation)
  }
  
  return exercises // 2-3 упражнения
}
```

#### Шаг 4: Определить параметры

```typescript
function setWorkoutParameters(exercise: Exercise, userHistory: WorkoutHistory) {
  const lastPerformance = getLastPerformance(exercise.id, userHistory)
  
  if (!lastPerformance) {
    // Первый раз делаем это упражнение
    return {
      sets: 3,
      reps: exercise.type === 'strength' ? '5' : '8-10',
      weight: null, // пользователь сам выберет
      rest: 90 // секунд
    }
  }
  
  // Если последний раз выполнил успешно - увеличиваем нагрузку
  if (lastPerformance.completed && lastPerformance.reps >= lastPerformance.targetReps) {
    return {
      sets: lastPerformance.sets,
      reps: lastPerformance.reps,
      weight: lastPerformance.weight + 2.5, // +2.5 кг
      rest: lastPerformance.rest
    }
  }
  
  // Иначе - повторяем с теми же параметрами
  return lastPerformance
}
```

### Примеры работы алгоритма:

**Пример 1: Новый пользователь (нет истории)**
```
Input: [] (пустая история)
Output: 
  - Squat (Приседания) - 3×5
  - Bench Press (Жим лежа) - 3×5
  - Barbell Row (Тяга в наклоне) - 3×8

Логика: Базовая программа Full Body с тремя основными движениями
```

**Пример 2: Последняя тренировка была Push**
```
Input: 
  - 2 дня назад: Bench Press, Overhead Press, Tricep Dips
Output:
  - Deadlift (Становая) - 3×5
  - Pull-ups (Подтягивания) - 3×8
  - Barbell Row (Тяга в наклоне) - 3×10

Логика: После Push делаем Pull, избегаем повтора упражнений
```

**Пример 3: Давно тренировали ноги**
```
Input:
  - 5 дней назад: Upper body workout
  - Ноги не тренировались 7 дней
Output:
  - Squat (Приседания) - 3×5
  - Romanian Deadlift (Румынская тяга) - 3×8
  
Логика: Приоритет недотренированным группам, 2 упражнения (минимализм)
```

## 🎯 Настройки и предпочтения пользователя

### Профиль минималиста:
```typescript
const minimalistProfile = {
  maxExercisesPerWorkout: 3, // не больше 3 упражнений
  preferredEquipment: ['barbell'], // предпочтение штанге
  preferCompoundExercises: true, // приоритет базовым
  avoidIsolation: true, // избегать изоляции
  workoutDuration: 'short', // короткие тренировки (30-45 мин)
  focusOnBigLifts: true // фокус на больших весах
}
```

### Система весов для выбора упражнений:

```typescript
function scoreExercise(exercise: Exercise, profile: UserProfile): number {
  let score = 0
  
  // Базовые упражнения получают +10 баллов
  if (exercise.movementPattern === 'compound') score += 10
  
  // Упражнения со штангой получают +5 баллов
  if (exercise.equipment === 'barbell') score += 5
  
  // Упражнения из "Big 5" получают +8 баллов
  if (['squat', 'deadlift', 'bench_press', 'overhead_press', 'barbell_row'].includes(exercise.id)) {
    score += 8
  }
  
  // Если упражнение делалось недавно - снижаем приоритет
  const daysSinceLastPerformance = getDaysSinceLastPerformance(exercise.id)
  if (daysSinceLastPerformance < 7) score -= 5
  if (daysSinceLastPerformance < 3) score -= 10
  
  return score
}
```

## 📊 Метрики и обратная связь

### Что отслеживать:
1. **Частота использования AI Suggested** - пользователь выбирает или игнорирует?
2. **Завершенность тренировок** - заканчивает ли предложенные тренировки?
3. **Прогресс** - растут ли веса при использовании AI рекомендаций?
4. **Разнообразие** - не повторяются ли упражнения слишком часто?

### Улучшение алгоритма со временем:
- Если пользователь часто пропускает определенные упражнения → снизить их приоритет
- Если пользователь добавляет свои упражнения → учесть их в будущем
- Если тренировки слишком тяжелые/легкие → скорректировать объем

## 🔄 Периодичность обновления

**Вариант 1: При входе на экран**
```typescript
// Каждый раз при открытии "AI Suggested" preset
useEffect(() => {
  if (activePreset === 'ai-suggested') {
    const suggested = generateAISuggestedWorkout(userHistory, userProfile)
    setAiSuggestedExercises(suggested)
  }
}, [activePreset])
```

**Вариант 2: Ежедневное обновление**
```typescript
// Генерировать один раз в день (кешировать)
const cacheKey = `ai-suggested-${today.toISOString().split('T')[0]}`
const cached = localStorage.getItem(cacheKey)
if (cached) {
  return JSON.parse(cached)
} else {
  const suggested = generateAISuggestedWorkout(userHistory, userProfile)
  localStorage.setItem(cacheKey, JSON.stringify(suggested))
  return suggested
}
```

**Вариант 3: После каждой завершенной тренировки**
```typescript
// При завершении тренировки пересчитать рекомендации
async function completeWorkout(workoutId: string) {
  await saveWorkout(workoutId)
  const newSuggestion = await generateAISuggestedWorkout(userHistory, userProfile)
  await updateAISuggested(newSuggestion)
}
```

**Рекомендация:** Вариант 1 (при входе на экран) - самый простой и актуальный.

## 📝 Итоговый алгоритм (псевдокод)

```typescript
function generateAISuggestedWorkout(
  userId: string,
  userProfile: UserProfile
): Exercise[] {
  // 1. Получаем историю за последние 14 дней
  const history = await getWorkoutHistory(userId, 14)
  
  // 2. Определяем, какие мышцы восстановились
  const readyMuscles = getMusclesReadyToTrain(history)
  
  // 3. Определяем тип тренировки на основе последних тренировок
  const workoutType = selectWorkoutType(readyMuscles, history)
  
  // 4. Получаем последние упражнения (чтобы не повторяться)
  const recentExercises = getRecentExercises(history, 2) // последние 2 тренировки
  
  // 5. Подбираем 2-3 упражнения
  const exercises = suggestExercises(
    workoutType,
    recentExercises,
    userProfile.equipment
  )
  
  // 6. Определяем параметры для каждого упражнения
  const workoutPlan = exercises.map(exercise => ({
    ...exercise,
    parameters: setWorkoutParameters(exercise, history)
  }))
  
  return workoutPlan
}
```

## 🚀 Следующие шаги

1. **Реализовать базовые функции:**
   - `getMusclesReadyToTrain()`
   - `selectWorkoutType()`
   - `suggestExercises()`

2. **Создать справочник упражнений:**
   - Категоризировать по типу (push/pull/legs)
   - Добавить метаданные (оборудование, сложность)
   - Определить "Big 5" и приоритетные упражнения

3. **Интеграция с UI:**
   - При клике на AI Suggested генерировать тренировку
   - Показывать объяснение: "Рекомендуем это потому что..."
   - Дать возможность регенерировать

4. **Тестирование:**
   - Проверить на разных сценариях
   - Убедиться, что не повторяются упражнения
   - Проверить логику восстановления

5. **Улучшения:**
   - Добавить машинное обучение для персонализации
   - Учитывать предпочтения пользователя
   - A/B тестирование разных алгоритмов

