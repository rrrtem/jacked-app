# Workout Exercise Type Fix ✅

## 🐛 Проблема

После изменений в системе тегов/категорий упражнений, в workout перестал показываться input для **веса** у упражнений с весами. Показывались только **повторы**.

### Причина

Функция `hasExerciseType()` проверяла **точное совпадение** типа упражнения:

```typescript
// СТАРАЯ ВЕРСИЯ (неправильно)
const hasExerciseType = (exercise, type) => {
  if (!exercise?.exercise_type) return false
  return exercise.exercise_type === type  // ❌ Только точное совпадение
}
```

Но в базе данных есть **несколько типов** упражнений с весом:
- `strength` - силовые упражнения (со штангой)
- `weight` - упражнения с весом
- `dumbbell` - упражнения с гантелями
- `barbell` - упражнения со штангой

Функция проверяла только на `"weight"`, поэтому упражнения типа `strength` и `dumbbell` определялись как упражнения **без веса**.

## ✅ Решение

Обновили функцию `hasExerciseType()` для проверки **нескольких вариантов**:

```typescript
// НОВАЯ ВЕРСИЯ (правильно)
const hasExerciseType = (exercise, type) => {
  if (!exercise?.exercise_type) return false
  
  // Для типа "weight" проверяем несколько вариантов
  if (type === 'weight') {
    return ['weight', 'strength', 'dumbbell', 'barbell'].includes(exercise.exercise_type)
  }
  
  return exercise.exercise_type === type
}
```

### Логика

**Для упражнений с весом (`type === 'weight'`):**
- ✅ `weight` - упражнения с весом
- ✅ `strength` - силовые упражнения
- ✅ `dumbbell` - с гантелями
- ✅ `barbell` - со штангой

**Для остальных типов:**
- Точное совпадение (как раньше)
- Например: `duration` для упражнений на время

## 🎯 Результат

### До исправления:
```
Упражнение: Bench Press (type: strength)
Показывается: [Reps: 10 times]           ❌ Только повторы
```

### После исправления:
```
Упражнение: Bench Press (type: strength)
Показывается: [Weight: 80 kg] [Reps: 10 times]  ✅ Вес И повторы
```

## 📊 Типы упражнений в системе

### С весом (показываются weight + reps):
| Тип | Описание | Пример |
|-----|----------|--------|
| `strength` | Силовые упражнения | Bench Press, Squat |
| `weight` | Упражнения с весом | Любые со штангой/весом |
| `dumbbell` | С гантелями | Dumbbell Press |
| `barbell` | Со штангой | Barbell Row |

### На время (показывается duration):
| Тип | Описание | Пример |
|-----|----------|--------|
| `duration` | На время | Plank, Wall Sit |

### Без веса (показываются только reps):
| Тип | Описание | Пример |
|-----|----------|--------|
| `bodyweight` | Собственный вес | Push-ups, Pull-ups |
| `warmup` | Разминка | Dynamic Stretching |
| `mobility` | Мобильность | Hip Circles |
| `stretching` | Растяжка | Hamstring Stretch |
| `cardio` | Кардио | Running, Cycling |

## 🔧 Где используется

Функция `hasExerciseType()` используется в нескольких местах:

1. **Определение типа инпутов** (строка 820, 867):
   ```typescript
   const isWeightExercise = hasExerciseType(exerciseData, "weight")
   const isDurationExercise = hasExerciseType(exerciseData, "duration")
   ```

2. **Рендеринг UI**:
   ```typescript
   {isWeightExercise && (
     <input type="text" placeholder="Weight (kg)" />
   )}
   ```

3. **Сохранение данных**:
   ```typescript
   if (isWeightExercise) {
     nextSet.weight = weight
   }
   ```

## ✅ Проверка

- ✅ TypeScript билд успешен
- ✅ Нет ошибок линтера
- ✅ Упражнения с типом `strength` показывают вес
- ✅ Упражнения с типом `dumbbell` показывают вес
- ✅ Упражнения с типом `weight` показывают вес
- ✅ Упражнения с типом `duration` показывают время
- ✅ Упражнения с типом `bodyweight` показывают только повторы

## 📝 Примеры работы

### Пример 1: Bench Press (strength)
```typescript
exercise = {
  name: "Bench Press",
  exercise_type: "strength"
}

hasExerciseType(exercise, "weight") // ✅ true
// Показывается: [Weight: __ kg] [Reps: __ times]
```

### Пример 2: Dumbbell Curl (dumbbell)
```typescript
exercise = {
  name: "Dumbbell Curl",
  exercise_type: "dumbbell"
}

hasExerciseType(exercise, "weight") // ✅ true
// Показывается: [Weight: __ kg] [Reps: __ times]
```

### Пример 3: Plank (duration)
```typescript
exercise = {
  name: "Plank",
  exercise_type: "duration"
}

hasExerciseType(exercise, "duration") // ✅ true
// Показывается: [Duration: __ sec]
```

### Пример 4: Push-ups (bodyweight)
```typescript
exercise = {
  name: "Push-ups",
  exercise_type: "bodyweight"
}

hasExerciseType(exercise, "weight") // ❌ false
// Показывается: [Reps: __ times]
```

## 🚀 Готово к использованию!

Теперь все упражнения с весом корректно отображают инпуты для **веса** и **повторов**.

