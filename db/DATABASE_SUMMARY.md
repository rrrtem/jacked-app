# База данных - Краткое резюме

## 📁 Файлы базы данных

| Файл | Описание |
|------|----------|
| `supabase_schema.sql` | **Основной SQL-скрипт** для создания всех таблиц, индексов и RLS политик |
| `DATABASE_SCHEMA.md` | Подробное описание всех таблиц, полей и связей |
| `DATABASE_DIAGRAM.md` | Визуальная диаграмма связей между таблицами |
| `SUPABASE_SETUP.md` | Пошаговая инструкция по настройке Supabase |
| `API_REFERENCE.md` | Справка по всем функциям для работы с БД |
| `MIGRATIONS.md` | Примеры миграций для будущих обновлений схемы |
| `seed_data.sql` | Дополнительные тестовые данные для разработки |
| `lib/types/database.ts` | TypeScript типы для всех таблиц |
| `lib/supabase/queries.ts` | Готовые функции для работы с данными |

## 🗂️ Структура базы данных

### 8 основных таблиц

```
1. users               → Профили пользователей
2. exercises           → Каталог упражнений
3. exercise_records    → Личные рекорды
4. workout_sets        → Шаблоны тренировок
5. workout_set_exercises → Упражнения в шаблонах
6. workout_sessions    → История тренировок
7. workout_session_exercises → Упражнения в сессии
8. workout_sets_data   → Данные подходов
```

### Ключевые связи

```
users
  ├─→ exercise_records (рекорды пользователя)
  ├─→ workout_sets (шаблоны пользователя)
  └─→ workout_sessions (история пользователя)

exercises
  ├─→ exercise_records (рекорды по упражнению)
  ├─→ workout_set_exercises (в шаблонах)
  └─→ workout_session_exercises (в сессиях)

workout_sessions
  └─→ workout_session_exercises
      └─→ workout_sets_data (подходы)
```

## 🚀 Быстрый старт

### 1. Создать проект в Supabase

```
1. Перейти на supabase.com
2. Создать новый проект
3. Дождаться инициализации (1-2 минуты)
```

### 2. Выполнить SQL-скрипт

```
1. Открыть SQL Editor в Supabase Dashboard
2. Скопировать содержимое supabase_schema.sql
3. Выполнить (Run или Ctrl+Enter)
```

### 3. Настроить переменные окружения

```env
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...
```

### 4. Использовать в коде

```typescript
import { getAllExercises } from '@/lib/supabase/queries'

const exercises = await getAllExercises()
```

## 📊 Примеры запросов

### Получить все упражнения

```typescript
import { getAllExercises } from '@/lib/supabase/queries'
const exercises = await getAllExercises()
```

### Создать тренировку

```typescript
import { createWorkoutSession } from '@/lib/supabase/queries'
const session = await createWorkoutSession(userId, {
  exercises: [
    { exercise_id: 'ex1', order_index: 0 },
    { exercise_id: 'ex2', order_index: 1 }
  ]
})
```

### Сохранить подход

```typescript
import { saveWorkoutSet } from '@/lib/supabase/queries'
await saveWorkoutSet({
  workout_session_exercise_id: 'wse-id',
  set_number: 1,
  weight: 50,
  reps: 10
})
```

### Получить историю

```typescript
import { getWorkoutHistory } from '@/lib/supabase/queries'
const sessions = await getWorkoutHistory({
  user_id: userId,
  date_from: '2025-01-01',
  limit: 10
})
```

## 🔒 Безопасность (RLS)

Все таблицы защищены Row Level Security:

- ✅ Пользователь видит только свои данные
- ✅ Упражнения доступны всем (READ-ONLY)
- ✅ Автоматическая проверка `auth.uid()`
- ✅ Каскадное удаление при удалении пользователя

## 📈 Производительность

### Индексы созданы для:

- Поиск пользователей по email
- Поиск упражнений по тегам (GIN индекс)
- Фильтрация по user_id (все таблицы)
- Сортировка по датам (история тренировок)
- JOIN операции (все foreign keys)

### Оптимизации:

- Использование UUID для всех ID
- TIMESTAMPTZ для работы с часовыми поясами
- Автоматическое обновление updated_at через триггеры
- Каскадное удаление для чистки данных

## 🧪 Тестовые данные

### Базовые (в supabase_schema.sql):

- 1 тестовый пользователь
- 10 базовых упражнений
- 1 шаблон тренировки "Full Body Workout"

### Расширенные (в seed_data.sql):

- 23 дополнительных упражнения
- 5 шаблонов (Push/Pull/Legs/Upper/HIIT)
- 12 тренировок за последний месяц
- Личные рекорды по 10 упражнениям

## 🛠️ Типичные операции

### Создание тренировки

```
1. createWorkoutSession()    → создать сессию
2. saveWorkoutSet()           → сохранить каждый подход
3. markWarmupCompleted()      → отметить разминку
4. completeWorkoutSession()   → завершить тренировку
5. updateExerciseRecord()     → обновить рекорды (опционально)
```

### Просмотр истории

```
1. getWorkoutCalendar()       → получить календарь за месяц
2. getWorkoutsByDate()        → тренировки за конкретную дату
3. getWorkoutSessionById()    → детали одной тренировки
```

### Работа с шаблонами

```
1. getWorkoutSets()                  → список всех шаблонов
2. getWorkoutSetWithExercises()      → детали шаблона
3. createWorkoutSet()                → создать новый
4. addExerciseToWorkoutSet()         → добавить упражнение
```

## 📝 Типы данных

Все типы определены в `lib/types/database.ts`:

```typescript
// Основные типы
Exercise, User, WorkoutSession, WorkoutSetData

// Типы для вставки
ExerciseInsert, WorkoutSessionInsert

// Типы для обновления
ExerciseUpdate, WorkoutSessionUpdate

// Расширенные типы с JOIN
ExerciseWithRecords, WorkoutSessionWithDetails
```

## 🔄 Миграции

Файл `MIGRATIONS.md` содержит 10 примеров миграций:

1. Добавление изображений упражнений
2. Категории упражнений
3. Заметки к упражнениям
4. Оценка сложности тренировки
5. Селфи после тренировки
6. Система целей (goals)
7. AI-рекомендации
8. Супер-сеты
9. Публичные шаблоны
10. Трекинг веса тела

## 🎯 Roadmap

### Текущая версия (v1.0)

- ✅ Базовая схема БД
- ✅ CRUD операции
- ✅ RLS политики
- ✅ TypeScript типы
- ✅ Готовые функции queries

### Планируется (v2.0)

- [ ] Категории упражнений
- [ ] AI-рекомендации
- [ ] Социальные функции (шаринг)
- [ ] Трекинг веса тела
- [ ] Система целей
- [ ] Фото прогресса

## 📚 Дополнительная информация

### Полезные команды SQL

```sql
-- Посмотреть все таблицы
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public';

-- Посмотреть структуру таблицы
\d exercises

-- Статистика тренировок
SELECT COUNT(*), AVG(duration)/60 as avg_minutes
FROM workout_sessions WHERE completed_at IS NOT NULL;
```

### Проверка данных

```sql
-- Количество упражнений
SELECT COUNT(*) FROM exercises;

-- Последние 5 тренировок
SELECT started_at, duration/60 as minutes 
FROM workout_sessions 
ORDER BY started_at DESC LIMIT 5;

-- Личные рекорды
SELECT e.name, er.max_weight, er.max_reps
FROM exercise_records er
JOIN exercises e ON e.id = er.exercise_id
WHERE er.user_id = 'your-user-id';
```

## 🆘 Помощь

### Проблемы?

1. Проверьте `.env.local` файл
2. Убедитесь, что таблицы созданы (Table Editor)
3. Проверьте RLS политики (Authentication → Policies)
4. Посмотрите логи (Logs → API)

### Ошибки?

- **JWT expired** → Обновите страницу
- **Permission denied** → Проверьте RLS политики
- **Relation does not exist** → Выполните SQL-скрипт
- **Connection refused** → Проверьте переменные окружения

### Вопросы?

- `DATABASE_SCHEMA.md` - описание схемы
- `API_REFERENCE.md` - справка по API
- `SUPABASE_SETUP.md` - настройка
- [Supabase Docs](https://supabase.com/docs)

## ✅ Чеклист настройки

- [ ] Создан проект в Supabase
- [ ] Выполнен SQL-скрипт `supabase_schema.sql`
- [ ] Проверено создание 8 таблиц
- [ ] Настроены переменные окружения
- [ ] Проверено подключение (тестовый запрос)
- [ ] Опционально: добавлены тестовые данные из `seed_data.sql`

## 🎉 Готово!

База данных настроена и готова к использованию!

Используйте функции из `lib/supabase/queries.ts` для работы с данными в вашем Next.js приложении.

