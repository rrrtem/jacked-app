# Настройка Magic Link для ngrok

Эта инструкция поможет вам настроить авторизацию через magic link при использовании ngrok туннеля для тестирования на мобильном устройстве.

## Проблема

При использовании magic link, Supabase отправляет ссылку с URL, который настроен в его конфигурации (обычно production URL на Vercel). Это не позволяет авторизоваться через ngrok туннель на телефоне.

## Что было исправлено в коде

1. ✅ Переключен на **Implicit Flow** вместо PKCE для мобильных устройств (`lib/supabase/client.ts`)
2. ✅ Включен middleware для обработки сессий (`middleware.ts`)
3. ✅ Улучшена обработка callback для implicit flow (`app/auth/callback/page.tsx`)
4. ✅ Добавлена поддержка переменной `NEXT_PUBLIC_SITE_URL` (`app/login/page.tsx`)

**Почему Implicit Flow?**
- PKCE требует, чтобы запрос и callback происходили в одном браузере
- На мобильных email-клиенты часто открывают ссылки во встроенном браузере
- Implicit flow работает без необходимости сохранять состояние между запросами

## Решение

### 1. Настройка Supabase Dashboard

1. Зайдите в [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **Authentication** → **URL Configuration**

4. **Добавьте Redirect URLs** (добавляется к существующим):
   ```
   https://your-subdomain.ngrok-free.dev/auth/callback
   ```
   Например:
   ```
   https://eyeable-unbreathing-phoebe.ngrok-free.dev/auth/callback
   ```

5. **Установите Site URL** (временно):
   ```
   https://your-subdomain.ngrok-free.dev
   ```
   ⚠️ **Не забудьте вернуть production URL после тестирования!**

6. **Проверьте Email Auth Provider**:
   - В разделе **Authentication** → **Providers**
   - Убедитесь, что **Email** provider включен
   - Проверьте, что **Enable email confirmations** ВЫКЛЮЧЕН (для magic link)
   - **Secure email change** должен быть включен

### 2. Создание .env.local

Создайте файл `.env.local` в корне проекта (если его нет):

```bash
# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# Site URL для Magic Link
# Используйте ngrok URL при тестировании на телефоне
NEXT_PUBLIC_SITE_URL=https://your-subdomain.ngrok-free.dev
```

### 3. Запуск приложения

```bash
# Остановите сервер если он запущен (Ctrl+C)

# Запустите заново
pnpm dev

# В другом терминале запустите ngrok
ngrok http 3000
```

⚠️ **Важно**: После изменения `.env.local` **обязательно перезапустите** Next.js сервер!

### 4. Тестирование на телефоне

1. Откройте ngrok URL на телефоне: `https://your-subdomain.ngrok-free.dev/login`
2. Введите email
3. Откройте почту на телефоне
4. Нажмите на ссылку в письме
5. Вы должны быть перенаправлены обратно в ngrok URL и авторизованы

## Важные замечания

### После тестирования

1. **Верните production URL** в Supabase Dashboard → Site URL
2. Удалите или закомментируйте `NEXT_PUBLIC_SITE_URL` в `.env.local`

### Альтернативный способ (без изменения Supabase Dashboard)

Если вы не хотите менять Site URL в Supabase каждый раз, можете:

1. В Supabase Dashboard → **Redirect URLs** добавьте ВСЕ возможные URLs:
   ```
   https://your-app.vercel.app/auth/callback
   http://localhost:3000/auth/callback
   https://*.ngrok-free.dev/auth/callback
   ```
   
2. Используйте `NEXT_PUBLIC_SITE_URL` в `.env.local` для переключения между окружениями

### Как работает код

В `app/login/page.tsx` теперь используется логика:

```typescript
const siteUrl = process.env.NEXT_PUBLIC_SITE_URL || window.location.origin

const { error } = await supabase.auth.signInWithOtp({
  email,
  options: {
    emailRedirectTo: `${siteUrl}/auth/callback?redirectTo=${encodeURIComponent(redirectTo)}`,
  },
})
```

Это означает:
- Если задана переменная `NEXT_PUBLIC_SITE_URL` - используется она
- Иначе используется текущий `window.location.origin` (работает для всех случаев)

## Troubleshooting

### ❌ "otp_expired" или "Email link is invalid or has expired"

**Возможные причины и решения**:

#### 1. Сервер не перезапущен после изменений
**Решение**:
1. ✅ **Остановите** сервер (`Ctrl+C`)
2. ✅ **Перезапустите**: `pnpm dev`
3. ✅ Запросите **НОВЫЙ** magic link

#### 2. Site URL в Supabase не совпадает с ngrok URL
**Решение**:
1. ✅ Откройте Supabase Dashboard → Authentication → URL Configuration
2. ✅ Проверьте **Site URL** = ваш ngrok URL
3. ✅ Проверьте **Redirect URLs** содержит `{ngrok-url}/auth/callback`
4. ✅ Подождите 1-2 минуты после изменения
5. ✅ Запросите новый magic link

#### 3. NEXT_PUBLIC_SITE_URL не установлен
**Решение**:
1. ✅ Проверьте файл `.env.local` в корне проекта
2. ✅ Должна быть строка: `NEXT_PUBLIC_SITE_URL=https://your-ngrok-url.ngrok-free.dev`
3. ✅ Перезапустите сервер

#### 4. Email confirmations включены в Supabase
**Решение**:
1. ✅ Supabase Dashboard → Authentication → Providers → Email
2. ✅ **Отключите** "Enable email confirmations"
3. ✅ Сохраните и запросите новый magic link

#### 5. Старый magic link
**Решение**: Magic links одноразовые и имеют срок действия (обычно 1 час). Запросите новый.

### Magic link ведет на старый URL

**Причина**: Supabase кэширует Site URL или `.env.local` не применился.

**Решение**:
1. Проверьте Site URL в Supabase Dashboard
2. Подождите 1-2 минуты после изменения
3. **Перезапустите Next.js сервер**
4. Попробуйте запросить новый magic link

### "Invalid redirect URL" ошибка

**Причина**: ngrok URL не добавлен в Redirect URLs.

**Решение**: Добавьте полный URL с `/auth/callback` в Redirect URLs в Supabase Dashboard

### Magic link не работает на телефоне

**Решение**: 
1. Откройте `/login` **на телефоне** через ngrok URL
2. Введите email **на телефоне**
3. Откройте почту **на телефоне**
4. Нажмите на ссылку **на телефоне**
5. Если ссылка открывается во встроенном браузере email-клиента, попробуйте скопировать ссылку и открыть в Chrome/Safari

💡 **Примечание**: Implicit flow должен работать даже если ссылка открывается в другом браузере, но некоторые встроенные браузеры могут блокировать куки.

### ngrok URL меняется при каждом запуске

**Решение**: 
- Используйте платную версию ngrok с постоянным доменом
- Или используйте другие сервисы (localtunnel, localhost.run)
- Или обновляйте URL в Supabase и `.env.local` при каждом запуске

## Для production

Перед деплоем на Vercel:
1. Убедитесь, что Site URL в Supabase указывает на production URL
2. Удалите `NEXT_PUBLIC_SITE_URL` из production переменных окружения
3. В Redirect URLs должен быть только production URL (и опционально localhost для разработки)

