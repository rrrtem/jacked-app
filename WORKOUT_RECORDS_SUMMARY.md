# Сводка изменений: Система рекордов и статистики тренировок

## 📊 Что было реализовано

### 1. ✅ Поле Duration в workout_sessions
**Статус**: Проверено и работает корректно
- Поле `duration` уже существовало и правильно сохраняет финальное время тренировки
- При нажатии кнопки **Finish** записывается значение `totalTime` (в секундах)
- Время начинает отсчитываться с момента начала тренировки и останавливается при завершении

### 2. ✅ Колонка total_volume в workout_sessions
**Добавлено**: Новое поле для хранения общего объема тренировки
- Хранит сумму всех: вес × количество повторов × подходы
- Считает только упражнения с весом (где есть тег `weight`)
- Автоматически рассчитывается при сохранении тренировки

```typescript
// Формула расчета
totalVolume = Σ (weight × reps) для всех подходов со всех упражнений
```

### 3. ✅ Система рекордов по упражнениям

#### 3.1. Таблица exercise_record_history
Создана новая таблица для хранения истории всех рекордов:
- `weight` - максимальный вес
- `reps` - количество повторений  
- `duration` - длительность (для упражнений на время)
- `workout_session_id` - ссылка на тренировку
- `achieved_at` - когда установлен рекорд

#### 3.2. Обновление рекордов
Функция `updateRecordsFromWorkout()` автоматически:
- Определяет тип упражнения по тегам
- Сравнивает результаты с текущими рекордами
- Обновляет максимальные значения в `exercise_records`
- Записывает новые рекорды в историю

#### 3.3. Типы отслеживаемых рекордов

**Для упражнений с тегом `duration`**:
- Отслеживается: максимальная длительность (в секундах)
- Пример: планка, статические удержания

**Для упражнений с тегом `weight`**:
- Отслеживается: максимальный вес + повторы при этом весе
- Пример: жим лежа 100кг × 8 повторений

**Для упражнений без веса**:
- Отслеживается: максимальное количество повторений
- Пример: отжимания 50 раз

### 4. ✅ Статистика пользователя

#### 4.1. Новые поля в таблице users
```sql
total_workouts INTEGER       -- Общее количество тренировок
total_weight_lifted DECIMAL   -- Весь поднятый вес за все время (кг)
```

#### 4.2. Автоматическое обновление
Создан триггер `trigger_update_user_workout_stats`, который при завершении тренировки:
- Увеличивает `total_workouts` на +1
- Добавляет к `total_weight_lifted` значение `total_volume` текущей тренировки

### 5. ✅ Обновленный финальный экран

Теперь показывает:
- **Total workouts** - общее количество тренировок пользователя
- **Session volume** - объем текущей тренировки (кг)
- **Lifetime volume** - весь поднятый вес за все время (кг)
- **Exercises completed** - количество упражнений в этой тренировке

## 📁 Измененные файлы

### База данных
- ✅ `db/migration_workout_records.sql` - SQL миграция со всеми изменениями
- ✅ `db/MIGRATION_INSTRUCTIONS.md` - подробные инструкции по применению

### TypeScript типы
- ✅ `lib/types/database.ts` - обновлены типы для новых полей

### Запросы к БД
- ✅ `lib/supabase/queries.ts` - добавлены функции:
  - `updateRecordsFromWorkout()` - обновление рекордов
  - `getUserStats()` - получение статистики пользователя

### UI компоненты
- ✅ `app/workout/[id]/page.tsx` - обновлены:
  - `saveWorkoutToDatabase()` - сохранение total_volume и вызов обновления рекордов
  - Финальный экран - отображение новой статистики
  - `useEffect` для загрузки статистики - получение данных из users

## 🚀 Как это работает

### Сценарий завершения тренировки:

1. **Пользователь нажимает Finish**
   ```typescript
   finishWorkout() → saveWorkoutToDatabase()
   ```

2. **Подсчет total_volume**
   ```typescript
   totalVolume = Σ (вес × повторы) для всех подходов
   ```

3. **Сохранение в БД**
   ```sql
   INSERT INTO workout_sessions (..., total_volume, duration, completed_at)
   ```

4. **Триггер автоматически обновляет users**
   ```sql
   UPDATE users SET 
     total_workouts = total_workouts + 1,
     total_weight_lifted = total_weight_lifted + total_volume
   ```

5. **Проверка и обновление рекордов**
   ```typescript
   updateRecordsFromWorkout(userId, sessionId, exerciseSets)
   ```

6. **Для каждого упражнения**:
   - Определяется тип по тегам (duration/weight/reps)
   - Находится максимальное значение в подходах
   - Сравнивается с текущим рекордом
   - Если новый рекорд → обновляется `exercise_records`
   - Записывается в `exercise_record_history`

7. **Отображение статистики**
   - Загружается `total_workouts` и `total_weight_lifted` из `users`
   - Показывается на финальном экране

## 📊 Примеры данных

### Пример workout_sessions после тренировки
```json
{
  "id": "uuid",
  "user_id": "user-uuid",
  "started_at": "2025-11-09T10:00:00Z",
  "completed_at": "2025-11-09T11:30:00Z",
  "duration": 5400,  // 1.5 часа в секундах
  "total_volume": 2450.5,  // кг
  "notes": "Total volume: 2451 kg"
}
```

### Пример exercise_records
```json
{
  "user_id": "user-uuid",
  "exercise_id": "bench-press-uuid",
  "max_weight": 100,
  "max_reps": 8,
  "last_updated": "2025-11-09T11:30:00Z"
}
```

### Пример exercise_record_history
```json
{
  "id": "record-uuid",
  "user_id": "user-uuid",
  "exercise_id": "bench-press-uuid",
  "workout_session_id": "session-uuid",
  "weight": 100,
  "reps": 8,
  "achieved_at": "2025-11-09T11:30:00Z",
  "notes": "New weight record: 100kg × 8 reps"
}
```

## 🔄 Следующие шаги для применения

1. **Примените миграцию** из `db/migration_workout_records.sql`
2. **Проверьте, что все таблицы и поля созданы** (инструкции в MIGRATION_INSTRUCTIONS.md)
3. **Перезапустите приложение** (если нужно)
4. **Протестируйте завершение тренировки**:
   - Создайте новую тренировку
   - Выполните несколько подходов с весом
   - Нажмите Finish
   - Проверьте финальный экран
   - Убедитесь, что статистика обновилась

## 🧪 Проверка работоспособности

После применения миграции и перезапуска:

```sql
-- 1. Проверить структуру
SELECT column_name FROM information_schema.columns 
WHERE table_name = 'workout_sessions' AND column_name = 'total_volume';

-- 2. Выполнить тренировку через UI

-- 3. Проверить данные
SELECT total_workouts, total_weight_lifted FROM users WHERE id = 'your-user-id';

-- 4. Проверить рекорды
SELECT * FROM exercise_records WHERE user_id = 'your-user-id';

-- 5. Проверить историю
SELECT * FROM exercise_record_history WHERE user_id = 'your-user-id' 
ORDER BY achieved_at DESC LIMIT 10;
```

## 💡 Технические детали

### Почему используется триггер?
- Гарантирует атомарность обновления
- Автоматически срабатывает при завершении тренировки
- Избавляет от необходимости вручную обновлять статистику в коде

### Почему отдельная таблица для истории?
- Позволяет отслеживать прогресс во времени
- Сохраняет все рекорды, не только текущие максимумы
- Можно строить графики прогресса

### Как работает определение типа рекорда?
```typescript
if (exerciseTags.includes('duration')) {
  // Ищем максимальную длительность
} else if (exerciseTags.includes('weight')) {
  // Ищем максимальный вес + повторы
} else {
  // Ищем максимальное количество повторов
}
```

## ⚠️ Важные замечания

1. **total_volume считается только для упражнений с весом**
   - Упражнения с тегом `duration` не учитываются
   - Упражнения без веса (только повторы) не учитываются

2. **Триггер срабатывает только при первом completed_at**
   - Если обновить уже завершенную тренировку, статистика не пересчитается дважды

3. **Рекорды обновляются асинхронно**
   - Если обновление рекордов упадет с ошибкой, тренировка все равно сохранится
   - Ошибка будет залогирована, но не прервет процесс

4. **Данные для существующих пользователей инициализируются**
   - Миграция автоматически подсчитывает статистику для уже завершенных тренировок
   - Новые пользователи начнут с нуля

## 📝 Заметки для будущего развития

### Возможные улучшения:
- [ ] Добавить отображение истории рекордов в UI
- [ ] Создать страницу с графиками прогресса
- [ ] Добавить уведомления о новых рекордах
- [ ] Реализовать экспорт статистики
- [ ] Добавить сравнение с другими пользователями (leaderboard)
- [ ] Реализовать цели и достижения (achievements)

### Оптимизации:
- [ ] Кэширование статистики пользователя
- [ ] Batch-обновление рекордов для больших тренировок
- [ ] Индексы для быстрых запросов истории

